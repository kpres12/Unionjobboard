{
  "name": "union-jobboard",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "sync-jobs": "npm run sync-jobs --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:python": "cd python-service && source venv/bin/activate && uvicorn main:app --reload --port 8000",
    "dev": "npm run seed && concurrently \"npm run dev:server\" \"npm run dev:python\" \"npm run dev:client\"",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
